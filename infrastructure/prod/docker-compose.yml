name: safesignal-prod

services:
  web:
    image: ${IMAGE:-ghcr.io/armlabhq/safesignal-website:latest}
    container_name: safesignal-prod-web
    restart: unless-stopped
    networks:
      - proxy
      - internal

networks:
  proxy:
    external: true
  internal:
